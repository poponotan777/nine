# 広告バナーの設定（ADS_JSON）

環境変数 `ADS_JSON` に、どのページにどのバナーを出すかを書きます。
未設定なら枠ごと消えるので、**1つずつ足していけば大丈夫です。**

---

## JSONとは

「キー（場所の名前）」と「値（バナーのHTML）」を組にして並べただけのものです。
決まりは4つだけ。

1. 全体を `{` と `}` で囲む
2. キーと値を `"` で囲む
3. キーと値の間は `:`
4. 組と組の間は `,`（**最後の組にはカンマを付けない**）

いちばん簡単な例（1つだけ設定する場合）:

```
{"common_rail":"<a href='...'>バナーのHTML</a>"}
```

2つ設定する場合:

```
{"book_inflow":"<バナーA>","movie_inflow":"<バナーB>"}
```

改行しても構いません。読みやすいのはこちらです:

```
{
  "book_inflow": "<バナーA>",
  "movie_inflow": "<バナーB>"
}
```

---

## キーの決まり

`{ページ}_{場所}_{言語}` の形です。言語は省略できます。

### ページ

| キー | どこ |
|---|---|
| `book` | 書籍モード |
| `manga` | 漫画モード |
| `cd` | CDモード |
| `anime` | アニメモード |
| `movie` | 映画モード |
| `person` | 有名人モード |
| `character` | キャラモード |
| `trends` | みんなの9つ |
| `common` | 上のどれにも指定が無いとき |

### 場所

| キー | どこ | 推奨サイズ |
|---|---|---|
| `rail` | PC画面の右側 | 160×600 |
| `rail2` | PC画面の左側 | 160×600 |
| `inflow` | 本文の中（スマホで見える） | 300×250 |
| `bar` | スマホ下部の固定バー | 320×50 |

### 言語

`_ja` を付けると日本語のときだけ、`_en` を付けると英語のときだけ表示されます。
付けなければ両方に出ます。

---

## 半々で出す

値を `[` `]` で囲んで並べると、そこからランダムで1つ選ばれます。
アフィリエイトとアドセンスを半々にしたいときはこう書きます。

```
{
  "book_inflow_ja": ["<A8のDMMブックス>", "<アドセンスのコード>"],
  "book_inflow_en": "<アドセンスのコード>"
}
```

**アドセンスが通るまでは配列にせず、A8のバナーだけを文字列で書いてください。**

---

## HTMLの中の " をどうするか

A8のバナーHTMLには `"` がたくさん含まれています。
JSONの中にそのまま貼ると壊れるので、**次のどちらかで対処します。**

### 方法1（おすすめ）: `"` を `'` に置き換える

メモ帳に貼り付けて、`"` をすべて `'` に置換してから使います。
HTMLは `'` でも動くので問題ありません。

### 方法2: `"` の前に `\` を付ける

`href="..."` を `href=\"...\"` にします。数が多いと大変です。

---

## 手順

1. A8の管理画面で「広告リンク作成」を押す
2. サイズを選んでHTMLをコピー
3. メモ帳に貼り、`"` を `'` に置換
4. 下のひな形の該当箇所に貼り付ける
5. できあがった全体を、Renderの環境変数 `ADS_JSON` に貼る
6. Save Changes（自動で再デプロイされます）

---

## 承認済み案件の配置とサイズ

| ページ | 場所 | 案件 | サイズ |
|---|---|---|---|
| **書籍** | `book_inflow` | DMMブックス | 300×250 |
| | `book_rail` | audiobook.jp | 160×600 |
| | `book_rail2` | 復刊ドットコム | 160×600 |
| **漫画** | `manga_inflow` | **漫画全巻ドットコム** | 300×250 |
| | `manga_rail` | DMMブックス | 160×600 |
| | `manga_rail2` | メルカリ | 160×600 |
| **CD** | `cd_inflow` | Audible | 300×250 |
| | `cd_rail` | パイオニア（オーディオ） | 160×600 |
| | `cd_rail2` | メルカリ | 160×600 |
| **映画** | `movie_inflow` | **TSUTAYA DISCAS** | 300×250 |
| | `movie_rail` | ゲオ宅配レンタル | 160×600 |
| | `movie_rail2` | WOWOWオンデマンド | 160×600 |
| **アニメ** | `anime_inflow` | **GAMERS** | 300×250 |
| | `anime_rail` | TSUTAYA DISCAS | 160×600 |
| | `anime_rail2` | ゲオ宅配レンタル | 160×600 |
| **キャラ** | `character_inflow` | **GAMERS** | 300×250 |
| | `character_rail` | メルカリ | 160×600 |
| **有名人** | `person_rail` | WOWOWプレミアム | 160×600 |
| **みんなの9つ** | `trends_rail` | TSUTAYA DISCAS | 160×600 |
| | `trends_inflow` | メルカリ | 300×250 |
| **全ページ共通** | `common_bar` | メルカリ | 320×50 |

置かない案件: Sycom（パソコン）、FlexiSpot（家具）、エアツリーマットレス、
DMM FX、IIJmio、ワイモバイル、ルミネ。文脈と無関係で数字も出ず、印象を損ないます。

A8で該当サイズが無い場合は、近いサイズ（`rail` なら 120×600 や 200×200、
`inflow` なら 336×280 や 468×60）で構いません。枠は自動で伸縮します。

---

## ひな形

まず2つだけ設定する例です。`<ここに...>` を実際のHTMLに差し替えてください。

```
{
  "book_inflow": "<ここにDMMブックスのバナー300x250>",
  "movie_inflow": "<ここにTSUTAYA DISCASのバナー300x250>"
}
```

慣れてきたら増やします。承認済みの案件での推奨配置:

```
{
  "book_inflow": "<DMMブックス 300x250>",
  "book_rail": "<audiobook.jp 160x600>",
  "book_rail2": "<復刊ドットコム 160x600>",

  "manga_inflow": "<漫画全巻ドットコム 300x250>",
  "manga_rail": "<DMMブックス 160x600>",
  "manga_rail2": "<メルカリ 160x600>",

  "cd_inflow": "<Audible 300x250>",
  "cd_rail": "<パイオニア（オーディオ） 160x600>",

  "movie_inflow": "<TSUTAYA DISCAS 300x250>",
  "movie_rail": "<ゲオ宅配レンタル 160x600>",
  "movie_rail2": "<WOWOWオンデマンド 160x600>",

  "anime_inflow": "<GAMERS 300x250>",
  "anime_rail": "<TSUTAYA DISCAS 160x600>",

  "character_inflow": "<GAMERS 300x250>",

  "trends_rail": "<TSUTAYA DISCAS 160x600>",
  "trends_inflow": "<メルカリ 300x250>",

  "common_bar": "<メルカリ 320x50>"
}
```

有名人モードには置いていません。売る商品が無いためです。

---

## うまくいかないとき

**広告が出ない**
`ADS_JSON` の書き方が壊れていると、すべて無視されます。
`{` と `}` で囲まれているか、最後の組にカンマが付いていないかを確認してください。

**確認方法**
ブラウザで `https://mynineloves.com/x/ads?kind=book&lang=ja` を開くと、
その種類で何が返っているか見られます。空なら設定が読めていません。

**JSONが正しいか調べる**
`https://jsonlint.com` に貼り付けると、間違いの箇所を教えてくれます。

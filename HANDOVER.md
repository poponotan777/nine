# MY NINE LOVES 引き継ぎ資料

最終更新: 2026年9月2日
本番: https://mynineloves.com
リポジトリ: https://github.com/poponotan777/nine

---

## 0. この人と作業するときの進め方

技術の引き継ぎより先に、ここを読んでほしい。
噛み合わせが悪いと同じ手戻りを繰り返すことになる。

### 相手の前提

- **コードは書かない。** こちらがファイルを用意し、相手は配置してコマンドを打つ役割。
  `git` も今回が初めてだった。
- **判断力は高い。** 「MyLinkは特定のページにしか飛ばないのでは」「CDも上4＋下5で
  試してみて」など、こちらの提案の穴を的確に突いてくる。その指摘はほぼ正しい。
- **自分で調べて動く。** APIキーの取得、ドメイン購入、DNS設定、アフィリエイト申請、
  Neonの構築はすべて自力で完了させている。手順さえ示せば実行できる。

### 守るべきこと

**ZIPは中身を検証してから渡す。**
過去に2回、古いZIPを渡して相手の時間を無駄にさせた。原因は `zip` の実行漏れ。
必ず `unzip -p` で変更が入っているか確認してから渡す。
相手も `Select-String -Path public\index.html -Pattern "変更点"` で確認できる。

**エラーが出たら、まず自分のミスを疑う。**
「置き換えてPUSHしましたが直ってない」と言われたら、9割こちらのミス。
相手の操作は正確。

**推測で「動くはず」と言わない。**
`inflow` がPCで非表示だった件、`/api/` がRenderの予約パスだった件、
楽天がRefererではなくOriginを見ていた件など、確認せず答えて外したことが複数ある。
分からないときは「確認します」と言って調べる。

**手順は1行ずつ提示する。**
PowerShellに複数行を貼ると連結されて壊れる事故が実際に起きた。
コマンドは1つずつコードブロックに分ける。

**日本語で、専門用語には説明を添える。**
JSONもDNSも「知らない前提」で説明したほうが早い。ただし子ども扱いはしない。
理由を説明すれば理解する人。

### 相手が嫌がること

- 長い前置き。結論から書く
- 「〜かもしれません」の連発。判断を示す
- 会話の履歴を忘れて同じ質問をすること

---

## 1. これは何か

好きなCD・漫画・書籍・アニメ・映画・有名人・キャラクターを9つ選ぶと、
1枚のカード画像になるツール。登録不要・無料。
`my9games.com`（私を構成する9つのゲーム）の構造を参考にしている。

収益はアフィリエイト。アドセンスは審査待ち。

---

## 2. 構成

```
nine-server/
├── server.js          ルーティング / キャッシュ / レート制限 / 画像プロキシ
│                      / 共有ページのHTML生成 / GA・LinkSwitch注入
├── providers.js       外部APIのアダプタ（検索・関連・購入リンク）
├── db.js              保存と集計（PostgreSQL / SQLite / JSON の3段フォールバック）
├── ads.js             広告の出し分け（アドセンスとアフィリエイトを別管理）
├── CLOUDFLARE.md      無料でできる負荷対策の手順（監視先の変更・Cloudflare）
├── ogimage.js         共有ページのサムネイル画像を生成
├── fonts/             日本語フォント2種（Renderに日本語フォントが無いため同梱）
├── package.json       依存は pg と @napi-rs/canvas
└── public/
    ├── index.html     エディタ本体（約73KB。UI・多言語・canvas書き出し）
    ├── trends.html    みんなの9つ / ランキング / カードの拡大表示
    ├── about.html     このサイトについて
    ├── terms.html     利用規約
    ├── privacy.html   プライバシーポリシー
    ├── contact.html   お問い合わせ（Googleフォームへ）
    └── icon-*.png     ファビコン（32/180/192/512）
```

Node 22以上。**Build Commandに `npm install` が必須**。

### インフラ

| | |
|---|---|
| ホスティング | Render（無料枠） |
| DB | Neon PostgreSQL（無料枠・シンガポール） |
| ドメイン | お名前.com（mynineloves.com） |
| 死活監視 | UptimeRobot（5分間隔・スリープ防止） |
| 解析 | Googleアナリティクス |

---

## 3. 完了していること

### 機能（実装・動作確認済み）

- 7モードの検索・選択・画像書き出し
- 作品名検索と作者検索の2系統。作者→作品一覧への遷移
- 関連作品（続編・シリーズ・同アーティスト・別の版）
- 表記ゆれ吸収（全角半角・ひらがなカタカナ・中黒・長音記号）
- 検索サジェスト（デバウンス420ms・前方一致キャッシュ・画面タップで閉じる）
- 種類ごとの選択保持＋端末への下書き保存（離れて戻っても続きから）
- 名前＋定型文でタイトルを合成（「太郎」＋「を構成する9冊」）
- 画像の書き出し（帯付き・種類ごとの縦横比）
- 共有ページ `/c/:id` とOGP画像 `/og/:id.png`
- 日英の言語切替（UIと外部API。英語では書籍モードを隠す）
- 作成数カウンタ、いいね、閲覧数
- `/trends`（3×3ランキング、10位以降、カード拡大、購入リンク）
- `/u/ユーザー名` で個人ページ
- 90日で未閲覧データを自動削除
- ファビコン

### 外部連携（確認済み）

- 楽天ブックス・Kobo（**403は解消済み**）
- AniList、TMDB、iTunes、楽天、Wikidata、Wikimedia Commons、Wikipedia
- LinkSwitch経由のYahoo!ショッピング
- Googleアナリティクスでの計測
- 楽天の Allowed websites に `mynineloves.com` を登録済み

### 環境変数（Renderに設定済み）

```
DATABASE_URL / IP_SALT / CONTACT_URL / CONTACT_MAIL
TMDB_API_KEY / RAKUTEN_APP_ID / RAKUTEN_ACCESS_KEY / RAKUTEN_AFFILIATE_ID
AMAZON_ASSOCIATE_TAG / VC_LINKSWITCH / VC_LINKSWITCH_ID / GA_ID / ADS_JSON
```

未設定: `ADSENSE_JSON`（審査通過後）

---

## 3.5 負荷対策（2026年9月に実施）

同種のサービス `my9games.com` が公開7日でアクセス集中により停止した事例を踏まえ、
先回りして手を入れた。詳細は `README.md` の「負荷への備え」。

| 弱点 | 対策 | 場所 |
|---|---|---|
| 画像でメモリを使い切って落ちる | 画像を別キャッシュに分け、**合計バイト数**で制限 | `imgCacheSet`（既定64MB） |
| 再起動でキャッシュが全部消える | 検索結果をDBの `kv` にも置く2段構え | `cacheGet2` / `cacheSet2` |
| 表記ゆれで同じ検索を何度も叩く | キャッシュキーを正規化 | `P.cacheKey` |
| 期限切れの瞬間に行列ができる | 古い内容を返し、裏で更新 | `refreshLater` |
| **同じ語を同時に検索すると人数分叩く** | 取得を1本にまとめる（30回→1回） | `once` |
| 同じ画像を同時に要求すると人数分取得 | 同上（20回→1回） | `fetchImageOnce` |
| 同じOGP画像を同時に生成する | 同上（15回→1回）。CPUを最も食う箇所 | `once('ogrender:')` |
| 作成数・一覧が毎回DBに問い合わせ | 30秒だけ持つ | `cacheGet('stats')` / `listKey` |
| 共有ページが毎回DBに問い合わせ | 5分持つ | `cacheGet('card:')` |
| 静的ファイルを毎回読み直す | 組み立て済みを保持 | `staticCache` |
| 1表示ごとに閲覧数をUPDATE | 3分ぶんをまとめて1回 | `countView` / `flushViews` |
| ランキングがカードと一緒に消える | 集計を `item_stats` に分離 | `db.js` |
| 同一IPの人がまとめて429になる | 全体は緩く、外部APIを叩く分だけ別枠で制限 | `rateLimited` |
| 監視だけで帯域を月676MB使う | `/healthz`（2バイト）を追加 | `server.js` |

### 同じ取得は1本にまとめている（once）

**これが拡散時に一番効く。** キャッシュが無い状態で同じ語を30人が同時に
検索すると、全員が「キャッシュに無い」と判断してから各自で外部APIを叩き、
1回で済むものが30回になっていた。1,000人来れば1,000回。
AniListの90req/分に対して桁が2つ違う。

`once(key, fetcher)` で取得中のキーを覚え、後から来た人には同じ約束を返す。
実測で30回→1回。同じ仕組みを次の3か所に入れてある。

| | 前 | 後 |
|---|---|---|
| 検索・サジェスト・作者・関連 | 同時人数分 | 1回 |
| 画像の取得（`fetchImageOnce`） | 20回 | 1回 |
| OGP画像の生成（`ogrender:`） | 15回 | 1回 |

OGP生成はCPUを最も食うので、無料枠の0.1CPUでは特に効く。

### キャッシュの効かせ方

**キーは `P.cacheKey()`（＝`normalize`）を通してから作る。**
素の入力をキーにすると「ＮＡＲＵＴＯ」「NARUTO」「NARUTO 」が別物になり、
同じ作品を探しているだけで外部APIを何度も叩く。実測で6回→2回になった。

**期限が切れても、古い内容をすぐ返して裏で取り直す**（stale-while-revalidate）。
作品名や表紙は1日古くても実害がない一方、応答を待たせると混雑時に行列ができる。
1500msかかる取得でも61msで返る。裏の取り直しは同時20件まで（`MAX_REFRESH`）。

保存は期限の6倍まで持ち、「本来の期限」は値の中の `soft` に入れてある。
こうすると `db.js` は素朴な期限付きKVのままでよい。

**唯一の「落ちる」要因だったのはメモリ。** 残りは「遅くなる」だけだった。
画像を本文の結果と同じキャッシュに入れていて、件数（4000件）でしか
制限していなかったのが原因。

### ランキングの持ち方が変わった

`card_items` を直接GROUP BYするのをやめ、`item_stats` に集計を持つようにした。
カードは90日で消えるが、ランキングは積み上げを残したいため。
**カードが消えても数字は残る。**

起動時に `item_stats` が空で `card_items` に行があれば、既存のカードから
自動で積み直す。ログに `ランキング集計を既存のカードから作り直しました` が出れば成功。

### 残っている上限

課金しても消えないものがある。

| | 上限 | 金で解決できるか |
|---|---|---|
| AniList | 約90req/分 | **不可能。**有料枠が存在しない |
| 楽天ブックス | 実質1req/秒 | **不可能** |
| Neon無料枠 | 月100 CU時間・0.5GB | Launchプラン（月8〜19ドル）で外れる |
| Render無料枠 | 0.1 CPU・512MB・帯域5GB | Starter $7 で 0.5 CPU |

**1日平均5,000〜8,000人が現状の目安。** 課金すれば2〜3万人まで伸びるが、
そこから先はAniListの制限が天井になり、いくら払っても伸びない。

---

## 4. 未完のこと

### 本番での確認が残っている

直近の大きな変更は反映済みだが、**一部は実機で未確認**。

| 確認項目 | 見るところ |
|---|---|
| 共有ページ | `/c/:id` を開いて9マスと購入リンクが出るか |
| OGP画像 | `https://www.opengraph.xyz/` に共有URLを貼る |
| `/trends` の刷新 | 3×3ランキング、カード拡大、いいね |
| ファビコン | タブのアイコン（キャッシュが強いので Ctrl+F5） |
| CDの検索 | 曲名やサウンドトラックで引けるか |
| CDのアフィリンク | 作品詳細に3つ出るか |

### すぐ着手できる

| 項目 | 状況 |
|---|---|
| **Xでの初投稿** | アカウント作成済み・**未投稿**。最優先 |
| **広告枠の残り** | `ADS_JSON` は一部のみ。`inflow2` や各モードの `rail` が空 |
| **アドセンス** | 審査待ち。通ったら `ADSENSE_JSON` に3枠入れるだけ |
| 英語版の規約類 | about / terms / privacy / contact が日本語のみ |
| **about の開発者名** | `（ここにあなたの名前とリンクを入れてください）` が本番に出たまま |
| 検索エンジンへの登録 | `robots.txt` と `sitemap.xml` は用意済み。Search Console への登録が未 |

### 設計判断が要る

| 項目 | 論点 |
|---|---|
| なりすまし対策 | Xのハンドルを誰でも入力できる。「未検証は `/u/` を作らない」方針まで決めて保留 |
| Blueskyのリアルタイム表示 | APIが無料。`#MyNineLoves` を `/trends` に流し込める |
| 地下アイドル等の網羅 | 公開APIでは不可能。ユーザー投稿型DBにするかの判断が保留 |
| 国別ランキング | 現状は言語別で代用。Cloudflare経由なら `CF-IPCountry` が無料 |

---

## 5. 重要な設計判断とその理由

### 外部APIをブラウザから直接叩かない
APIキーが露出し、外部画像を描いたcanvasが汚染されて画像を書き出せなくなる。
サーバー経由にすることで、この2つとレート制限を同時に解決している。

### 画像は保存せずURLだけを持つ
保存すると複製・再配布にあたり、多くのAPIの規約に触れる。
`card_items.image_url` にURL文字列だけを記録。

### アップロード画像はサーバーに送らない
端末内のみで処理。画像共有には含まれるが、共有ページには出さない。
権利リスクの遮断が目的。共有ページにもその旨を明記している。

### 年齢ではなく生まれ年を保存
年齢のまま持つと毎年ずれ、一斉更新の運用が必要になる。
入力は年齢のまま、保存時に `今年 - 年齢` で変換。

### タイトルは「名前＋定型文」で合成
入力欄は「なまえ」→「つづき」の順（完成形と同じ並び）。
保存時は合成後の文字列を入れる。古い下書きが二重にならないよう
`stripName()` で先頭の名前を取り除いてから合成する。

### 共有は2択に分ける
「3×3の画像で保存」と「カードのURLでシェア」。
以前は画像とURLを同時に送っていて、送り先で崩れた。
画像共有では画像だけを送り、本文はクリップボードに控える。

### 広告を置く場所を限定
作る流れと共有の導線には出さない。共有されなければアクセスが伸びず、
結果として収益も伸びないため。置くのは作品の詳細・本文の下部・
左右のレール・`/trends` のみ。

### スマホ広告はポップアップにしない
Googleがインタースティシャル広告をペナルティ対象にしているため、
下部固定バー（閉じるボタン付き）にしている。

### OGP画像は上4＋下5
3×3だと縦長の表紙が小さくなる。2段5列なら高さに余裕ができ、
本来の縦横比のまま大きく見せられる。正方形（CD）だけは
文字を上に置いてグリッドを横幅いっぱいに広げる。

### APIの節約

外部APIを叩く回数は、次の4段で抑えている。

1. **キャッシュキーを正規化** — 「ＮＡＲＵＴＯ」「NARUTO」「NARUTO 」を同じ1件として扱う
2. **同じ語の取得は1本にまとめる**（`once`）— 同時に何人来ても1回。画像とOGP生成も同様
3. **2段キャッシュ** — メモリ＋DBの `kv`。再起動しても消えない（検索24時間・サジェスト7日）
4. **期限切れでも待たせない** — 古い内容を返し、裏で取り直す

さらに、必要なときだけ追加で叩く仕組みがある。

- 表記ゆれの追撃検索（`fanout`）は、結果が5件未満のときだけ動く
- 有名人はWikidataとTMDBで4件以上取れたら、Wikipedia全文検索を呼ばない
- 書籍は楽天ブックスで8件以上取れたら、楽天Koboを呼ばない

実測では、結果が十分なら1回の検索で外部APIは**1回**、
薄くて追撃が走った場合でも**2回**に収まっている。

---

## 6. ハマりどころ（実際に踏んだもの）

| 症状 | 原因 |
|---|---|
| `/api/*` が404 | **Renderが `/api/` を予約パスとして横取りする**。`/x/` に変更済み |
| 楽天が400 `specify valid applicationId` | 2026年2月にAPI刷新。`openapi.rakuten.co.jp` + accessKey が必須 |
| 楽天が403 `HTTP_REFERRER_MISSING` | **文面はRefererだが実際は `Origin` が必要**。両方送って解決 |
| Wikipediaが429 | Wikimedia系のキューを共有していた。1200ms間隔＋連絡先入りUAで解決 |
| 有名人検索に別人が混ざる | Wikipedia全文検索は人物判定をしない。Wikidataの `P31=Q5` で解決 |
| 表紙が見切れる | AniListの表紙は2:3ではなく **460:654**。実寸に合わせ `contain` に |
| CDにアフィリンクが出ない | 内部の種別名は `cd` ではなく **`album`**。判定表に両方書く |
| CDだけ絞り込みが空になる | 同じ原因。`/trends` のボタンが `cd` を送って400。`album` に修正済み |
| 監視だけで帯域を食う | トップ（82KB）を5分間隔で叩くと月676MB。`/healthz` に変える |
| ローカルで `table cards has no column` | 古い `data/nine.db` が残っている。**`data` を消せば直る** |
| ADS_JSONが反映されない | HTMLの `"` と**改行**。`'` に置換して1行にする |
| OGPに日本語が出ない | Renderに日本語フォントが無い。`fonts/` に同梱して登録 |
| ヘッダーが invalid header value | 旧版の `RAKUTEN_REFERER` の扱い。現在は複数書ける（上から順に試す） |

---

## 7. 日常の運用

### コードを更新する
```
git add .
git commit -m "何を直したか"
git push
```
pushするとRenderが自動で再デプロイ（3〜5分）。

### ローカルで動かす
```
Remove-Item -Recurse -Force data
node server.js
```
DBの構造を変えた版に差し替えたら、`data` を消してから起動する。

### 環境変数を変える
Renderのダッシュボード → Environment → 編集 → Save Changes。
保存すると自動で再デプロイ。pushは不要。

### 広告を足す
1. A8で広告リンクを作成しHTMLをコピー
2. メモ帳で**改行を削除**し、`"` を `'` に置換
3. `ADS_JSON` に `"キー名": "HTML"` の形で追加（最後の項目にカンマ不要）
4. `https://jsonlint.com` で検証してから貼る

詳細は `ADS-SETUP.md`。

### 確認用URL

| | |
|---|---|
| `/x/config` | APIキーの設定状況 |
| `/x/stats` | 作成数 |
| `/x/ads?kind=book&lang=ja` | その種類で返る広告 |
| `/healthz` | 死活監視用。`ok` だけを返しDBに触れない |
| `/x/top?type=manga` | ランキングの生データ |
| `/x/card?id=xxx` | カード1件の中身 |
| `/og/xxx.png` | OGP画像 |

---

## 8. 次にやるべきことの優先順位

0. **`CLOUDFLARE.md` の2作業**（費用ゼロ・合計30分）
   UptimeRobotの宛先を `/healthz` に変更し、前段にCloudflareを置く。
   **拡散させる前に済ませること。** Renderは無料アカウントでも
   帯域超過を課金する（$0.15/GB）ため、放置すると覚えのない請求につながる。
1. **本番での動作確認**（4章の表を上から順に）
2. **`/trends` にデータを溜める** — 各モード9つずつ自分で作る。空の一覧は印象が悪い
3. **Xで初投稿** — 自分で9つ選んだ画像を添えて固定ポストに
4. **広告枠を埋める** — まず `inflow2` から
5. **アドセンス通過後の設定** — `ADSENSE_JSON` に3枠
6. **技術記事を書く** — 「日本語の書影APIが軒並み死んだ2026年に」という切り口

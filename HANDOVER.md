# MY NINE LOVES 引き継ぎ資料

最終更新: 2026年9月3日
本番: https://mynineloves.com
リポジトリ: https://github.com/poponotan777/nine
開発者: [@POPONOTAN_neet](https://x.com/POPONOTAN_neet)

---

## 0. この人と作業するときの進め方

技術の引き継ぎより先に、ここを読んでほしい。
噛み合わせが悪いと同じ手戻りを繰り返すことになる。

### 相手の前提

- **コードは書かない。** こちらがファイルを用意し、相手は配置してコマンドを打つ役割。
  `git` も今回が初めてだった。
- **判断力は高い。** 「MyLinkは特定のページにしか飛ばないのでは」「CDも上4＋下5で
  試してみて」など、こちらの提案の穴を的確に突いてくる。その指摘はほぼ正しい。
- **自分で調べて動く。** APIキーの取得、ドメイン購入、DNS設定、アフィリエイト申請、
  Neonの構築はすべて自力で完了させている。手順さえ示せば実行できる。

### 守るべきこと

**ZIPは中身を検証してから渡す。**
過去に2回、古いZIPを渡して相手の時間を無駄にさせた。原因は `zip` の実行漏れ。
必ず `unzip -p` で変更が入っているか確認してから渡す。
相手も `Select-String -Path public\index.html -Pattern "変更点"` で確認できる。

**エラーが出たら、まず自分のミスを疑う。**
「置き換えてPUSHしましたが直ってない」と言われたら、9割こちらのミス。
相手の操作は正確。

**推測で「動くはず」と言わない。**
`inflow` がPCで非表示だった件、`/api/` がRenderの予約パスだった件、
楽天がRefererではなくOriginを見ていた件など、確認せず答えて外したことが複数ある。
分からないときは「確認します」と言って調べる。

**手順は1行ずつ提示する。**
PowerShellに複数行を貼ると連結されて壊れる事故が実際に起きた。
コマンドは1つずつコードブロックに分ける。

**日本語で、専門用語には説明を添える。**
JSONもDNSも「知らない前提」で説明したほうが早い。ただし子ども扱いはしない。
理由を説明すれば理解する人。

### 相手が嫌がること

- 長い前置き。結論から書く
- 「〜かもしれません」の連発。判断を示す
- 会話の履歴を忘れて同じ質問をすること
- **同じ間違いを繰り返すこと。** レール広告の追従は4回やり直した。
  直したつもりで直っていないときは、実測で確かめてから渡す
- スクリーンショットの読み違い。「UIの言語」と「ランキングの絞り込み」を
  取り違えて的外れな修正をしたことがある。何を操作した画面かを確認する

---

## 1. これは何か

好きなCD・漫画・書籍・アニメ・映画・有名人・キャラクターを9つ選ぶと、
1枚のカード画像になるツール。登録不要・無料。
`my9games.com`（私を構成する9つのゲーム）の構造を参考にしている。

収益はアフィリエイト。アドセンスは審査待ち。

---

## 2. 構成

```
nine-server/
├── server.js          ルーティング / キャッシュ / レート制限 / 画像プロキシ
│                      / 共有ページのHTML生成 / GA・LinkSwitch注入
├── providers.js       外部APIのアダプタ（検索・サジェスト・関連・作者・購入リンク）
├── db.js              保存と集計（PostgreSQL / SQLite / JSON の3段フォールバック）
├── ads.js             広告の出し分け（アドセンスとアフィリエイトを別管理）
├── ogimage.js         共有ページのサムネイル生成（1200×630）
├── fonts/             日本語フォント2種。Renderに無いため同梱
├── tools/
│   └── make-og-site.js  トップページ用のOGP画像を作り直すスクリプト
├── package.json       依存は pg と @napi-rs/canvas の2つだけ
└── public/
    ├── index.html     エディタ本体（UI・多言語・canvas書き出し）
    ├── trends.html    みんなの9つ / ランキング / カードの拡大表示
    ├── about.html     このサイトについて（日英併記）
    ├── terms.html     利用規約（日英併記）
    ├── privacy.html   プライバシーポリシー（日英併記）
    ├── contact.html   お問い合わせ（日英併記）
    ├── robots.txt     クローラー向け。**/og/ と /img の Allow を消さないこと**
    ├── ads.txt        アドセンスの認証（1行）。無いと配信が絞られる
    ├── sitemap.xml    固定ページのみ
    ├── og-site.png    トップページ用のOGP画像
    └── icon-*.png     ファビコン
```

Node 22.5 以上。**`npm install` が必要**（OGP生成に `@napi-rs/canvas` を使う）。

### インフラ

| | |
|---|---|
| ホスティング | Render（無料枠） |
| DB | Neon PostgreSQL（無料枠） |
| ドメイン | お名前.com |
| 前段 | Cloudflare（`CLOUDFLARE.md`） |
| 死活監視 | UptimeRobot → `/healthz` |
| 解析 | Googleアナリティクス |

---

## 3. 環境変数

| キー | 用途 |
|---|---|
| `DATABASE_URL` | PostgreSQL接続文字列。あればこれが保存先になる |
| `IP_SALT` | IPアドレスのハッシュ化。**公開環境では必須** |
| `DATA_DIR` | SQLite/JSONの置き場（既定 `data/`） |
| `KEEP_DAYS` | 未閲覧データの保持日数（既定90） |
| `PORT` | 待ち受けポート（既定3000） |
| `TMDB_API_KEY` | 映画・人物 |
| `RAKUTEN_APP_ID` / `RAKUTEN_ACCESS_KEY` | 書籍。2026年2月の刷新でaccessKeyが必須化 |
| `RAKUTEN_REFERER` | 楽天に送るOrigin/Referer。複数書ける（上から順に試す） |
| `RAKUTEN_AFFILIATE_ID` | 楽天リンクのアフィリエイト化 |
| `AMAZON_ASSOCIATE_TAG` | Amazon検索リンク |
| `VC_LINKSWITCH` / `VC_LINKSWITCH_ID` | バリューコマースのLinkSwitch |
| `VOD_LINKS` | 映画・アニメの配信サービス導線（JSON） |
| `CONTACT_URL` | 楽天APIに送る自サイトURL。UserAgentにも入る |
| `CONTACT_MAIL` | Wikimedia向けUserAgentの連絡先 |
| `GA_ID` | Googleアナリティクス。あればHTMLに自動で差し込まれる |
| `ADS_JSON` | アフィリエイトのバナー（`ADS-SETUP.md`） |
| `ADSENSE_JSON` | アドセンス。**通常は設定しない。** リポジトリの `adsense.json` に書く（`ADS-SETUP.md`）。この変数を設定するとファイルより優先される。**コードは `pub-7335055001091712` のアカウントのものを使う** |
| `IMG_CACHE_MB` | 画像キャッシュの上限MB（既定64） |
| `RATE_PER_MIN` | IPあたりの全体上限（既定600） |
| `UPSTREAM_PER_MIN` | IPあたり、外部APIを叩く回数の上限（既定120） |
| `RENDER` | **自分で設定しない。** Renderが自動で入れる。これがあるのに保存先がPostgreSQLでないとき、起動ログで警告を出す（`db.js`） |

キーはコードに一切書かず、環境変数だけで渡す設計。

---

## 4. エンドポイント

**`/api/` は使えない。** Renderが予約パスとして横取りするため、すべて `/x/`。
ここを戻すと本番で全部404になる。

| | |
|---|---|
| `GET /x/search?type=&q=&lang=` | 作品名で検索。購入リンク付き |
| `GET /x/suggest?type=&q=` | 入力補完 |
| `GET /x/creators?type=&q=` | 作者・アーティストの候補 |
| `GET /x/works?type=&id=&author=` | その人の作品一覧 |
| `GET /x/related?type=&id=` | 続編・シリーズ・同アーティスト |
| `GET /x/config` | 各モードが有効かどうか |
| `GET /x/stats` | 作成数（30秒キャッシュ） |
| `POST /x/cards` | カードを1件保存。**9つ揃っていないと記録しない** |
| `GET /x/cards?type=&notype=&sort=&handle=&q=` | 一覧（30秒キャッシュ） |
| `GET /x/card?id=` | カード1件。呼ぶと閲覧数が増える |
| `GET /x/top?type=&notype=&lang=&decade=&age=` | 作品ランキング |
| `POST /x/like` | いいね（端末ごとに1カード1回） |
| `GET /img?u=` | 許可ホストの画像だけを中継 |
| `GET /og/:id.png` | 共有用サムネイル（保存時に先に作る） |
| `GET /healthz` | 死活監視用。`ok` だけ返しDBに触れない |

ページは `/` `/trends` `/about` `/terms` `/privacy-policy` `/contact`
`/c/:id`（共有ページ）`/u/ユーザー名`（個人ページ）。

### 内部の種別名に注意

CDモードの内部名は `cd` ではなく **`album`**。
`ADS_JSON` のキーは `cd_` なので `/trends` 側で読み替えている。
過去に「CDだけ絞り込みが空」「CDだけ広告が出ない」の2件を踏んでいる。

---

## 5. データの持ち方

| 条件 | 保存先 |
|---|---|
| `DATABASE_URL` がある | PostgreSQL（Neon等）。公開環境はこれ |
| 無い場合 | `node:sqlite`（`data/nine.db`）。ローカル開発はこれ |
| SQLiteも使えない | JSON（`data/nine.json`） |

テーブルは4つ。`cards`（本体）、`card_items`（9つの中身）、
`item_stats`（ランキング用の集計）、`kv`（APIキャッシュ＋いいねの記録）。

**ランキングを `card_items` から直接集計していない。寿命が違うため。**
カードは90日で消えるが、ランキングは積み上げを残したい。
`item_stats` は保存時に `(種類, 提供元, 作品ID, 言語, 生まれた年)` ごとに
1ずつ加算する。**カードが消えても数字は残る。**

起動時に `item_stats` が空で `card_items` に行があれば自動で積み直す。
ログに `ランキング集計を既存のカードから作り直しました` が出る。

容量は1枚あたり約3.1KB。Neonの0.5GBで**約17万枚**。

---

## 6. 主要な仕様

### 9つ揃わないと保存も共有もできない

**サーバー側（`/x/cards`）で9つ未満を弾いている。**
画面側でもボタンを無効にし、押されたときも確認しているが、
そこだけだと `/x/cards` に直接POSTすれば虫食いのカードを保存できてしまう。
`item_stats` は積み上げなので、一度混ざった数字は取り消せない。

画面側の関数名は `recordCard`（`index.html`）。サーバー側とは別物。

### 規約類は1ファイルに日英を両方入れている

`/en/terms` のような別URLにはしていない。
ルーティングと `sitemap.xml` と `robots.txt` を触る必要が出るためで、
**`robots.txt` は `/og/` の Allow を壊すとXのサムネイルが消える**箇所なので、
触らずに済ませる判断をした。

仕組みは `<html lang>` によるCSSの出し分け。
`html[lang="ja"] [data-tr="en"]{display:none}` の1行で切り替わる。

**本文の出し分けは `data-tr`。`data-lang` ではない。**
言語ボタン（`.langopt`）が `data-lang` を使っているため、
本文側も `data-lang` にすると**英語表示のときに「日本語」ボタン自体が消える**。
実際に一度踏んだ。

ボタンは `.head-meta` > `.langsw` > `.langopt` で、
つくるページ・みんなの9つと同じマークアップとCSSを使っている。
片方を直したらもう片方も直すこと。

### 書体は丸ゴシック（M PLUS Rounded 1c）

もとは見出しが明朝体で、硬い印象だった。2026年9月に丸ゴシックへ変更。

- **画面側**: Google Fonts から読み込む。`<head>` の `<link>` と、
  `--mincho` `--gothic` の中身。**6ページ＋共有ページの計7か所**にある。
  `display=swap` にしてあるので、読み込みが遅れても文字は消えない。
- **カード画像**: `fonts/MPLUSRounded1c-*.ttf` を同梱し `ogimage.js` で登録。
  読めなければ Noto に落ちる（Noto も残してある）。
  **ここを変え忘れるとカード画像だけ書体が違う**ことになる。

`--mincho` という変数名は残っているが、中身は明朝ではない。
使われている箇所（見出し・カードのタイトル）を全部たどるのが面倒なため、
名前はそのままにした。

### ヘッダーの見た目は6ページで同じ値に揃えてある

`index` `trends` `about` `terms` `privacy` `contact` のすべてで、

```
.head-text{padding:18px 16px}
.head-text h1{font-weight:700;font-size:23px;letter-spacing:.04em;margin:0 0 4px}
.head-text p {margin:0;font-size:12px;color:#A9B6A2;letter-spacing:.04em}
```

もとは 22px/600/.06em と 23px/700/.04em が混在していて、
ページを移動するたびに見出しの大きさが変わっていた。

帯の高さは **`header{min-height:92px}` で固定**してある。
中身の行数で決まる作りにすると、どこか1ページの文言を足し引きするたびに
帯の高さが変わり、ページを移動したときタブの位置が動く。
実際に「副題を足す」「枚数を足す」で二度やり直した。

左右とも `justify-content:center` で縦中央に置くので、
**行数が違うページがあっても見た目は揃う。**
`ABOUT` にはヘッダーの枚数表示が無いが、本文に「これまでに作られた数」が
あって二重になるため意図的に外している。高さは他と同じ。

`header` は `align-items:stretch` のままにすること。
縦帯（`.obi-tab`）が min-height まで伸びなくなる。

**揃わない原因は CSS の数値ではなく、次の2つだった（実測して判明）。**

1. **`body{zoom}` が規約類に無かった。**
   つくるページとみんなの9つには
   `@media (min-width:1024px){ body{zoom:1.10} }` と `1600px で 1.15` が掛かっている。
   規約類には無く、PCで見たときだけ1割小さく描かれていた。
   `font-size:23px` を揃えても、zoom が違えば見た目は揃わない。

2. **`body{line-height:1.9}` がヘッダーにも継承されていた。**
   規約類は本文を読みやすくするため body に 1.9 を掛けている。
   これが `h1` と副題にも効き、行が高くなっていた。
   `.head-text{line-height:normal}` で戻したうえで、
   `h1` に `1.34`、副題に `1.36` を明示して本家と一致させた。

**見た目が揃わないときは、CSSの値を見比べるだけでは足りない。**
`zoom` と、body からの継承（line-height、font-family）を必ず疑うこと。
実際にブラウザで `getBoundingClientRect().height` を測るのが確実。

**枚数の文言の切り替え方はページによって違う。**
`index.html` と `trends.html` は `applyLang()` がJSで書き換える方式なので、
`data-tr` の span を置いても**両方の言語が同時に出てしまう**（一度踏んだ）。
この2ページは `I18N` に文言を足して `applyLang` から書き込む。
規約類4ページは `data-tr` でよい。
言語の判定スクリプトは **`<head>` に置く**。`<body>` の描画前に確定させないと、
日本語が一瞬見えてから英語に入れ替わる。
記憶先は `localStorage` の `'lang'` で、つくるページ・みんなの9つと同じキー。
**JSが無効でも `<html lang="ja">` のままなので日本語版が出る。**

文言を足すときは**両方の言語に足すこと。** 片方だけ足すと、
言語を切り替えたときにその段落が消えたように見える。

### 生まれた年をそのまま聞き、そのまま保存する

以前は「年齢」を聞いて `今年 - 年齢` で変換していたが、
入力時点の年で結果が変わるうえ、誕生日前後で1歳ずれる。
いまは**生まれた年を直接聞く**。つくるページに「生まれた年を変える」のリンクがある。

集計も10年区切りをやめ、生まれた年をそのまま持つ。
「同年代」は**前後1年**を含める（1999年生まれなら1998〜2000年）。
10年区切りだと1999年と2000年の生まれが分断されるため。

**注意**: 2026年9月以前のカードの `age_band` は10年区切りの値。
そのぶんは同年代ランキングに出にくい。

### いいねは端末ごとに1カード1回、一度きり

ブラウザは端末の識別子を出さないので、初回に乱数でIDを作り `localStorage` に保存する。
IPで数えると、同じ回線を共有している人（スマホのCGNAT）が押せなくなるため。
記録は `kv` に10年で残すので、再起動しても押し直せない。
IDを保存できない設定のときだけIPに落ちる。同じIDからは1分20件まで。

### 書籍は日本語専用

楽天ブックスに依存しているため、英語のカードには存在しない。
UIが英語、またはランキングの絞り込みが「英語」のときは書籍のボタンを隠す。
逆に書籍を選んでいるときは「英語」を隠す。
**選択は勝手に戻さない**（押した覚えのない状態に変わると戸惑わせるため）。

### 画像は保存せずURLだけを持つ

保存すると複製・再配布にあたり、多くのAPIの規約に触れる。

### アップロード画像はサーバーに送らない

端末内のみで処理。書き出す画像には含まれるが、共有ページでは**そのマスが空**になる。
権利リスクの遮断が目的。この挙動は画面上でも説明している。

### 広告は5枠、置く場所を限定

`inflow` `inflow2`（本文内）`rail` `rail2`（PC左右）`bar`（スマホ下部）。
本文内はアフィリエイト優先、左右と下部はアドセンス優先。
規約類には置かない。詳細は `ADS-SETUP.md`。

**共有ページ（`/c/:id`）には `rail` `rail2` `bar` を置いている**（2026年9月に追加）。
もとは置かない方針だったが、ここはSNSからの着地点でいちばん人が来るため。
本文内（`inflow`）は、カードを見に来た人の視線を遮るので置いていない。
共有ページの広告はサーバー側でHTMLに直接書き出す（画面側の `runAdScripts` は通らない）。
ページ名は `share` なので、`share_rail` のように書けばここだけ差し替えられる。

アドセンスのキーは `{ページ}_{場所}` でも引ける。
`person_inflow` のように書けば、そのページだけに出せる。

---

## 7. 負荷への備え

同種のサービス（my9games.com）が公開7日で停止した事例を踏まえている。

| 弱点 | 対策 | 場所 |
|---|---|---|
| 同じ語を同時に検索すると人数分叩く | 取得を1本にまとめる（30回→1回） | `once` |
| 同じ画像・OGPを同時に要求 | 同上（20回→1回、15回→1回） | `fetchImageOnce` |
| 表記ゆれで同じ検索を何度も叩く | キャッシュキーを正規化 | `P.cacheKey` |
| 期限切れの瞬間に行列 | 古い内容を返し裏で更新 | `refreshLater` |
| 再起動でキャッシュが消える | DBの `kv` にも置く2段構え | `cacheGet2` |
| 画像でメモリを使い切る | 合計バイト数で制限（既定64MB） | `imgCacheSet` |
| ランキングがカードと一緒に消える | 集計を分離 | `item_stats` |
| 同一IPの人がまとめて弾かれる | 外部APIを叩く分だけ別枠 | `rateLimited` |
| 一覧・作成数が毎回DBへ | 30秒だけ持つ | `server.js` |
| 共有ページが毎回DBへ | 5分持つ | `cacheGet('card:')` |
| 1表示ごとに閲覧数をUPDATE | 3分ぶんをまとめて1回 | `countView` |
| 監視だけで帯域を月676MB | `/healthz`（2バイト） | `server.js` |

実測: 200人が一巡する1200リクエストで、**外部API 1回、DB問い合わせ4回、1.1秒**。

**1日平均5,000〜8,000人が目安。** 課金すれば2〜3万人まで伸びるが、
そこから先はAniListの90req/分が天井で、いくら払っても伸びない。

---

## 8. ハマりどころ（実際に踏んだもの）

| 症状 | 原因 |
|---|---|
| `/api/*` が404 | **Renderが `/api/` を予約パスとして横取りする** |
| 楽天が400 | 2026年2月にAPI刷新。`openapi.rakuten.co.jp` + accessKey が必須 |
| 楽天が Referer エラー | 文面はRefererだが、実際に見ているのは **Origin** |
| Wikipediaが429 | Wikimedia系のキューを共有。1200ms間隔＋連絡先入りUA |
| AniListが403 | **AniList側の全面停止**。Jikan → Kitsu に自動で切り替わる |
| CDだけ絞り込み・広告が空 | 内部の種別名が **`album`**。`cd` と混同していた |
| Xでサムネイルが出ない | **`robots.txt` で `/og/` を禁止していた**。Xは規約を守る（Discordは見ないので出る）。**`robots.txt` を触るときは必ず X Card Validator か実ポストで確認する** |
| モーダルの文字が重なる | `.out` に `sheet` クラスが無く背景が透明だった |
| 広告の下に大きな余白 | A8の**1×1の計測用画像**に `width:100%` が効いて274px四方に伸びた |
| 共有ページだけGAに出ない / Yahooの成果が付かない | GAとLinkSwitchの差し込みは `serveStatic()` の中にあり、**`public/` の静的HTMLにしか効かない**。`/c/:id` のHTMLは `server.js` が組み立てるので、`${GA_TAG}${VC_TAG}` を `</head>` の前に自分で書く必要がある。**共有ページはSNSからの着地点で、いちばん人が来る場所**。ここが漏れると流入も成果も丸ごと落ちる |
| アドセンスが「アカウントが承認されませんでした」 | **アドセンスは1人1アカウント。** 別メールで新規に取った `pub-7498884122648069` が、既存の承認済みアカウントと重複して弾かれた。**使うのは承認済みの `pub-7335055001091712` のほう。** 既に承認されているので、サイトを追加するだけでよく、アカウント審査のやり直しは要らない（サイト単位の審査は別途ある）。重複したほうは閉鎖する。**閉鎖は取り消せないので、閉じるのが未承認の新しい側であることを必ず確認してから押す** |
| レールが追従しない | **`body{zoom}` の内側では `position:sticky` が効かない**。JSで計算している |
| レール幅を変えたら横スクロールバーが出た | `body` に `zoom` が掛かるので **body幅 = 画面幅 ÷ zoom**。レールの外端が body 幅を超えると溢れる。条件は **画面幅 ≥ 2×(オフセット+レール幅)×zoom**。幅を変えたらメディアクエリの閾値も上げること。現在は index が 幅160・オフセット300・閾値1100、trends が 幅160・オフセット400・閾値1280 |
| レールにアドセンスを入れたのに空欄 | **120px幅はアドセンスの在庫がほとんど無い**。160×600（ワイドスカイスクレイパー）が正規サイズ。2026年9月に120px→160pxへ変更済み |
| ランキングの大きさが不安定 | `getBoundingClientRect().top` はスクロール位置で変わる。`+ scrollY` で解決 |
| 英語→日本語でタイトルが混ざる | プレビューを更新したあとに入力欄を書き換えていた |
| いいねが何度も押せる | 印を検索結果と同じキャッシュに入れていて押し出されていた |
| ローカルで `table cards has no column` | 古い `data/nine.db` が残っている。**`data` フォルダを消せば直る** |
| カードを消したのにランキングが残る | **`item_stats` はカードとは別の表**で、カードが90日で消えても数字は残り続ける（そういう設計）。`cards` だけ消しても効かない。さらに**`item_stats` だけ消すと、次の再起動で `db.js` がカードから積み直して復活する。** 両方を同時に消す必要がある。`tools/reset-cards.js` がそれをやる |
| ADS_JSONが反映されない | HTMLの `"` と**改行**。`'` に置換して1行にする |
| Renderで環境変数が保存できない | アドセンスのコードは1900文字あり、環境変数に入れると保存に失敗することがある。**`adsense.json` に書けば環境変数は不要**（`ads.js` が環境変数→ファイルの順で読む） |
| アドセンスの枠が空のまま | **`innerHTML` で入れた `<script>` はブラウザが実行しない**（HTML5の仕様）。アフィリエイトは `<a><img>` だけなので動いていた。`runAdScripts()` で `<script>` を作り直して入れ替えている。**`index.html` と `trends.html` の両方にある。片方だけ直すとそちらだけ出ない** |
| アドセンスが在庫切れでも空欄のまま | `ads.js` の優先度は**キーの有無しか見ない**ので、配信されなかった場合は切り替わらない。`watchUnfilled()` が `data-ad-status="unfilled"` を見て、10秒待ってから `*Alt`（アフィリエイト）に差し替える |

---

## 9. 日常の運用

### コードを更新する

```
git add .
git commit -m "何を直したか"
git push
```

pushするとRenderが自動で再デプロイ（3〜5分）。

### 環境変数を変える

Renderのダッシュボード → Environment → 編集 → Save Changes。
保存すると自動で再デプロイ。pushは不要。

### 確認用URL

| | |
|---|---|
| `/healthz` | 生きているか（`ok` が返る） |
| `/x/config` | APIキーの設定状況 |
| `/x/stats` | 作成数 |
| `/x/ads?kind=book&lang=ja` | その種類で返る広告 |
| `/x/top?type=manga` | ランキングの生データ |

### OGP画像を作り直す

```
node tools/make-og-site.js
```

---

## 10. 残っている作業

### すぐできる

| 項目 | 状況 |
|---|---|
| **Xでの初投稿** | **9つ揃えて「カードのURLでシェア」から投稿すること**。トップのURLを貼ると、9つではなくサイト紹介が出る |
| Search Console への登録 | プロパティ登録済み（アナリティクス経由で所有権が自動確認された）。`sitemap.xml` の送信が未 |
| アドセンスのサイト審査 | メタタグ（`ca-pub-7335055001091712`）は全ページと `/c/:id` に配置済み。アドセンス画面で「確認」→「審査をリクエスト」が未 |
| アドセンス通過後の設定 | `ADSENSE_JSON` に `rail` `rail2` `bar` の3つを書くだけ。**審査に通るまで設定しない** |
| 広告枠の残り | `character_inflow2` が空 |


### 設計判断が必要

| 項目 | 論点 |
|---|---|
| なりすまし対策 | Xのハンドルを誰でも入力できる。「未検証は `/u/` を作らない」まで決めて保留 |
| Blueskyのリアルタイム表示 | APIが無料。`#MyNineLoves` を `/trends` に流し込める |
| 地下アイドル等の網羅 | 公開APIでは不可能。ユーザー投稿型にするかの判断が保留 |
| 国別ランキング | Cloudflareを置いたので `CF-IPCountry` が使える |
| 楽天の購入リンク | 保存時のアフィリエイトIDが焼き付いている。IDを変えると過去分は古いまま |

これらは**いま決めなくてよい。** 拡散して反応を見てからのほうが判断材料が揃う。
